# Memory — Append-Only Execution Log

Continuity across sessions. **Append only.** Never rewrite or prune past entries
(`invariants.md` § Documentation). One entry per meaningful change.

Format: `YYYY-MM-DD — <what changed> | scope: <files/feature> | why: <reason>`

---

2026-06-14 — Established `.claude/` orchestration architecture (invariants.md, conventions.md, this log, FEATURES/{bundles,roles-permissions}/{purpose,rules}.md), mirroring the MathPro_Frontend repo's system. | scope: .claude/**, AGENTS.md | why: prevent context decay + scope-creep refactors as the admin console grows; give agents a routing map into bundles/RBAC surfaces and the existing docs/ guides.

2026-07-19 — Added `is_free` toggle to the course create/edit form (Free Course switch next to Price Fields, mirroring the existing `is_live` Switch pattern). Wired through `CourseFormData` (types/course.types.ts), `course-form-mapper.ts` defaults/denormalization, `CourseForm.tsx` submit payload, and `CreateCourseData`/`UpdateCourseFullData`/`Course` types (services/course.service.ts, types/index.ts). `price`/`x_price` remain required/unchanged when the toggle is on — kept as historical/display data. | scope: components/course/{CourseBasicInfoForm,CourseForm}.tsx, types/course.types.ts, types/index.ts, lib/course-form-mapper.ts, services/course.service.ts | why: backend added `course.is_free` (Math_Pro_backend 2026-07-19) so admin can mark a course free-of-charge for lead-gen; students then enroll via the existing checkout modal with no payment. Combos/bundles deliberately out of scope for this pass — same pattern, deferred. Contract: backend `POST /user/course/enroll-free/:id` (student-facing, MathPro_Frontend consumes it) and admin `is_free` field on the existing V2 course CRUD endpoints (additive, no breaking change).
2026-06-14 — Wired the courses-page featured-course admin surface to the backend-managed featured-course API so admins can choose which courses appear in the public `/courses` slider. | scope: featured-course management UI / courses-page slider admin controls | why: replace implicit frontend ordering with explicit admin-controlled course featuring.
2026-06-14 — Replaced manual numeric ordering with drag-and-drop sorting on FAQ management and testimonial management, and stabilized testimonial state syncing to avoid the React maximum-update-depth loop. | scope: `/faq-management`, `/testimonial-management`, hooks/useTestimonials.ts | why: improve admin UX for ordered content and eliminate the infinite re-render caused by unstable featured-row syncing.
2026-06-14 — Fixed combo detail/edit routes to read the actual `[comboid]` param so `/combos/2` no longer trips the invalid-ID guard. | scope: app/(dashboard)/combos/[comboid]/{page.tsx,edit/page.tsx} | why: align route param handling with the App Router folder name.
2026-06-14 — Normalized single-bundle admin API responses and switched bundle form writes to the enhanced endpoints; combo detail/edit now read embedded courses instead of a missing `/admin/bundle/:id/courses` route. | scope: services/bundle.service.ts, lib/constants.ts, app/(dashboard)/combos/[comboid]/page.tsx, components/bundles/BundleForm.tsx | why: match the real backend contract and remove false assumptions that broke combo detail/edit flows.
2026-06-14 — Updated `/combos` admin-facing copy to use Combo/Slug labels while keeping backend `bundle` / `url` identifiers intact. | scope: app/(dashboard)/combos/**, components/bundles/**, context.md, .claude/FEATURES/bundles/**, .claude/invariants.md | why: align the admin UI terminology with the product language and avoid mislabeling the slug field as a URL.
2026-06-14 — Aligned repo-level admin guidance with the combo terminology rule so AGENTS no longer frames Combo as student-frontend-only copy. | scope: AGENTS.md | why: remove contradictory documentation after standardizing the `/combos` admin surface on Combo/Slug labels.
2026-06-14 — Replaced the custom combo detail/form top bars with the shared page-header treatment so `/combos`, `/combos/new`, `/combos/[id]`, and `/combos/[id]/edit` use the same header language and action layout. | scope: components/bundles/ComboPageHeader.tsx, components/bundles/BundleForm.tsx, app/(dashboard)/combos/[comboid]/page.tsx | why: keep the combo authoring flow visually consistent with the list page and remove one-off header chrome.
2026-06-14 — Replaced the featured-courses slider serial input/save flow with drag-and-drop ordering and direct publish toggles. | scope: app/(dashboard)/courses/page.tsx, hooks/useFeaturedCourses.ts | why: match the FAQ/testimonial drag-sort UX and remove manual order entry from the featured slider table.
2026-06-14 — Added slug-aware combo detail/edit loading on the admin `/combos` surface and exposed a managerial `/admin/bundle/slug/:slug` endpoint while keeping writes keyed by numeric bundle id. | scope: app/(dashboard)/combos/[comboid]/**, components/bundles/BundleForm.tsx, hooks/useBundles.ts, services/bundle.service.ts, Math_Pro_backend/routes/controllers/managerial/bundle.js | why: let `/combos/test-combo` resolve correctly without breaking existing `/combos/2` flows or update/export/course-assignment endpoints.
2026-06-14 — Updated course create/edit basic-info placeholders and tag guidance to use MathPro school-exam examples instead of university-coded copy. | scope: components/course/CourseBasicInfoForm.tsx | why: align the admin authoring UI with SSC/JSC/HSC audiences and keep create/edit form copy consistent.
2026-06-14 — Renamed the course metadata bundle block to Combo and commented out the extra metadata editor in the course form. | scope: components/course/CourseMetadataForm.tsx | why: match admin vocabulary and temporarily hide the editable meta section.
2026-06-14 — Hid the student feedback section in course edit mode while keeping it available for new course creation. | scope: components/course/CourseForm.tsx | why: remove feedback editing from existing course forms without changing create flow.
2026-06-14 — Removed the student feedback section from the shared course form entirely. | scope: components/course/CourseForm.tsx | why: hide the feedback editor from both create and edit flows.
2026-06-14 — Replaced the custom course create/edit sticky header with the shared `PageHeader` pattern used by `/courses`. | scope: components/course/CourseForm.tsx | why: keep the course authoring header visually aligned with the reusable courses page header.
2026-06-14 — Added draft book selection to the course create flow and attached the selected books automatically after the course is saved. | scope: components/course/CourseForm.tsx, components/course/CourseLinkedBooks.tsx, app/(dashboard)/courses/new/page.tsx | why: let admins prepare linked books during course creation instead of waiting until edit mode.
2026-06-14 — Reverted the draft book selection flow and hid the books tab from course creation again. | scope: components/course/CourseForm.tsx, components/course/CourseLinkedBooks.tsx, app/(dashboard)/courses/new/page.tsx | why: keep linked books edit-only and remove the tab from `/courses/new`.
2026-06-14 — Fixed the video module PDF source switch so React no longer reuses a controlled text input as a file input. | scope: components/course/ModuleEditor/forms/VideoModuleForm.tsx | why: eliminate the uncontrolled-to-controlled input warning in the module editor.

2026-06-14 — Removed the Assignment module type + module-instructor link + chapter phase + assignment-submission analytics, tracking the backend's migration 014 contract changes. Deleted `AssignmentModuleForm.tsx`, the `app/(dashboard)/courses/[courseId]/assignments/**` submissions page, `PhaseSelector.tsx`, and `analytics-v2/SubmissionAnalytics.tsx`. Removed `ASSIGNMENT` from `ModuleCategory` + `ModuleTypeSelector`/`ModuleItem` icon map; removed the module-instructor `<Select>` from `BaseModuleForm` and `instructor_id` from all 5 module forms' create/update payloads; stripped `phase` state/usages from the course page; removed `useUploadAssignmentDocument`/`useDeleteAssignmentDocument`/`useModuleInstructor`/`useAssignInstructor` hooks + their `module.service.ts` functions; removed `users_with_submissions`/`CourseEngagement.submissions`/SubmissionAnalytics* types + the `useSubmissionAnalytics` hook + service method; removed `submissions` from student history (`user.service.ts` types + user-detail page). Left COURSE-level `InstructorManagement` (CourseForm) untouched — that's the course `instructor` table, unrelated to the dropped `module.instructor_id`. `tsc --noEmit` clean. Note: `npm run lint` (`next lint`) mis-parses cwd in this env and errors with "no such directory: …/lint" — pre-existing tooling quirk, not from these changes. | scope: types/index.ts, types/analytics-v2.types.ts, components/course/**, app/(dashboard)/{page,courses/[courseId]/page,users/[id]/page}.tsx, hooks/{useModules,useAnalyticsV2}.ts, services/{module,chapter,course,analytics-v2,user}.service.ts | why: track backend migration 014 (assignment feature + module instructor + chapter phase removed; quiz kept).

2026-06-14 — Phase 4 Live-Class toggle (Admin). Added a "Live Class" toggle to `VideoModuleForm` (status SCHEDULED/LIVE/ENDED, scheduled-at datetime, Zoom meeting id + passcode) sent via the v2 update-enhanced mutation; `live_*` fields added to `Module` type + the `updateModuleEnhanced` service signature. Authoring flow: create a VIDEO module, then toggle it live on edit; after class set status=ENDED and fill the Video URL so it plays as a normal recording. Live overlay is a separate mechanism from the existing `live`-table live-classes feature (per user decision). | scope: components/course/ModuleEditor/forms/VideoModuleForm.tsx, types/index.ts, services/module.service.ts | why: support running a zoom live class as a module then dropping in the recording, no type-switch.

2026-06-14 — Removed CODE module type (Admin), tracking backend CODE removal. Deleted `CodeModuleForm.tsx` + `CodeProblemBuilder.tsx`; removed CODE from `ModuleCategory`, `ModuleTypeSelector`, `ModuleEditor` (branch+import), `ModuleItem` (icon+Code2 import), and the CODE icon-case/name-inference + `faCode` import in `ModuleProgressModal`. `tsc --noEmit` clean. | scope: components/course/ModuleEditor/**, components/course/ModuleItem.tsx, components/students/ModuleProgressModal.tsx, types/index.ts | why: CODE module type removed product-wide.

2026-06-15 — Rebranded the admin repo copy from CoderVai/Binary School to Math Pro and initialized the local git repo for the GitHub push. | scope: README.md, docs/**, course.md, next.config.js, .env.example, app/**, components/**, .claude/memory.md | why: prepare the admin site for the `parvej-shah/mathpro_admin.git` remote with consistent Math Pro branding.
2026-06-16 — Updated the student details view to use the new fixed student profile keys from backend and removed login-facing exposure from the student detail summary. | scope: services/student.service.ts, components/students/StudentDetails.tsx, components/students/StudentProfileHeader.tsx, app/(dashboard)/users/[id]/page.tsx | why: keep the admin student profile aligned with the new profile contract while hiding login from the visible student UI.
2026-06-16 — Updated module row badges so `is_live` reads as Published/Draft and `live_status` shows a separate Live Class status badge. | scope: components/course/ModuleItem.tsx | why: align the module list with the edit modal wording and expose live-class state without conflating it with publish state.
2026-06-16 — Adjusted the module live-class badge labels/colors so upcoming uses warning, live uses destructive red, and ended stays muted. | scope: components/course/ModuleItem.tsx | why: match the requested live-class badge semantics in the module list.
2026-06-16 — Updated chapter publish badges to use Published/Draft instead of Live/Draft and removed the leading dot. | scope: components/course/ChapterItem.tsx | why: keep chapter status wording aligned with the module publish state.
2026-06-16 — Renamed the chapter create/edit modal live checkbox label to Publish. | scope: app/(dashboard)/courses/[courseId]/page.tsx | why: align chapter authoring copy with the publish semantics used elsewhere in the course UI.
2026-06-16 — Added a global thin, theme-tinted scrollbar style for the admin UI. | scope: app/globals.css | why: replace the browser default scrollbar with one that matches the admin theme across pages and overlays.
2026-06-17 — Added a storage public URL env hook to `next.config.js` so `next/image` accepts the new R2/custom storage host, while keeping the existing S3 host allowlist for backwards compatibility. Added `NEXT_PUBLIC_STORAGE_PUBLIC_URL_BASE` to `.env.example` so the upload flow can be pointed at the backend-issued R2 public URL base. | scope: next.config.js, .env.example | why: wire the admin upload/display path to the backend's new storage host without changing the upload API contract.
2026-06-17 — Replaced the native browser alt-text prompt in the Lexical rich-text toolbar with a themed dialog and restored the saved Lexical editor state before insert so uploaded quiz/announcement images still land at the original cursor position. `tsc --noEmit` clean. | scope: components/announcements/LexicalToolbar.tsx | why: align image-alt entry with the admin theme without breaking image insertion after the dialog steals focus.
2026-06-17 — Made the shared Lexical editor upload purpose configurable and pointed quiz question/option/explanation editors at `quiz-image` instead of the announcements prefix. `tsc --noEmit` clean. | scope: lib/s3-upload.ts, components/announcements/{LexicalEditor,LexicalToolbar}.tsx, components/course/ModuleEditor/forms/QuizBuilder.tsx | why: rich-text image uploads in the quiz builder should land in a quiz-specific storage folder, not `announcements/`.
2026-06-17 — Replaced the course metadata thumbnail URL inputs with immediate S3 image uploads for `course_thumbnail_16_9`, `trailer_video_thumb_16_9`, and `facebook_community_thumb_16_9`. `tsc --noEmit` clean; `npm run lint` still fails because `next lint` is not compatible with the current Next/ESLint setup in this repo. | scope: components/course/CourseMetadataForm.tsx | why: let admins upload thumbnails directly instead of pasting links on course create/edit.
2026-06-17 — Moved the thumbnail asset uploads out of the Details tab into the Media tab, leaving Details focused on non-media course metadata. `tsc --noEmit` clean. | scope: components/course/CourseForm.tsx, components/course/CourseMetadataForm.tsx | why: keep the course editor sections semantically grouped and remove the redundant thumbnails block from metadata.
2026-06-17 — Moved the three thumbnail upload fields onto a dedicated Thumbnails tab and removed the separate course-cover Media uploader. `tsc --noEmit` clean. | scope: components/course/CourseForm.tsx, components/course/CourseMetadataForm.tsx | why: match the requested editor layout and keep only the three thumbnail uploads surfaced in their own tab.
2026-06-17 — Removed the course header helper tools dialog/button and deleted the now-unused helper component. `tsc --noEmit` clean. | scope: components/course/CourseForm.tsx, components/course/CourseHeaderHelpers.tsx | why: the helper upload/date dialog was no longer needed after moving thumbnail uploads into the dedicated Thumbnails tab.
2026-06-17 — Wired course thumbnail replacements/clears to delete the previous storage object via the new backend `POST /v2/admin/upload/delete` endpoint, using the current public URL as the delete identifier. `tsc --noEmit` clean. | scope: components/course/CourseMetadataForm.tsx, lib/s3-upload.ts | why: avoid orphaned thumbnail objects when admins replace or clear a course thumbnail. Clients affected: mathpro_admin + Math_Pro_backend.
2026-06-17 — Added a course-outline mode switch in the basic info form so admins can either paste a link or upload a PDF; PDF uploads use the new `course-outline` storage purpose and replace/delete the previous uploaded file when the outline changes. `tsc --noEmit` clean. | scope: components/course/CourseBasicInfoForm.tsx, lib/s3-upload.ts | why: let admins choose between external outline links and uploaded PDF outlines without changing the stored `course_outline` string contract.
2026-06-17 — Removed the `Course Sections` editor from the course Details tab, leaving enrollment details, social links, and combo metadata only. `tsc --noEmit` clean. | scope: components/course/CourseMetadataForm.tsx | why: hide the unused section editor from course create/edit while keeping the rest of the metadata form intact.
2026-06-17 — Changed course thumbnail previews to keep the local blob preview visible until the returned storage public URL is actually loadable; if the remote URL cannot be rendered yet, the UI now keeps the working local preview and warns instead of showing a broken image. `tsc --noEmit` clean. | scope: components/course/CourseMetadataForm.tsx, .claude/memory.md | why: production thumbnail uploads were succeeding in R2 but the admin UI was swapping too early to an unavailable public image URL.
2026-06-17 — Added explicit `next/image` remote host allowlisting for Cloudflare R2 public hosts `*.r2.dev`, `*.r2.de`, and `*.r2.cloudflarestorage.com` in addition to the env-driven storage host. | scope: next.config.js, .claude/memory.md | why: production course thumbnails were present in API chips but not rendering because the returned R2 public host was outside the Next image optimizer allowlist.
2026-06-18 — Removed the email channel from announcement authoring and grid status rendering so admin announcements only send and show web notifications. | scope: components/announcements/AnnouncementForm.tsx, components/announcements/AnnouncementGrid.tsx, services/announcement.service.ts | why: align the admin surface with the backend's notification-only announcement delivery path.
2026-06-18 — Updated the announcements list filter to treat `notification_is_sent` as the only sent state. | scope: app/(dashboard)/announcements/page.tsx | why: the backend no longer treats email as a delivery channel for announcements.
2026-06-18 — Removed the recipient and channel controls from the announcement form; the admin UI now always creates student-only web notifications and only edits subject/message/course. | scope: components/announcements/AnnouncementForm.tsx | why: match the backend-enforced announcement contract and stop exposing choices that are no longer supported.
2026-06-19 — Added a live rich-text preview under the announcement editor so admins can see the rendered body, including uploaded images, before sending. | scope: components/announcements/AnnouncementForm.tsx | why: make announcement composition match the final rendered output without changing the backend contract.

2026-06-18 — Admin auth is now centralized on the student Frontend (no admin login form). lib/auth.ts: added FRONTEND_AUTH_URL (NEXT_PUBLIC_FRONTEND_AUTH_URL, default https://www.mathpro.academy) + getFrontendLoginUrl(), persistToken() (shared .mathpro.academy cookie + localStorage), captureTokenFromUrl() (adopts a token from the `#token` hash handed back by the Frontend login, else the shared cookie, then strips the hash); logout() now clears the shared cookie and bounces to the Frontend login. AuthContext.refreshUser() calls captureTokenFromUrl() on boot. app/(auth)/login/page.tsx is now a pure redirector to the Frontend login. app/middleware.ts redirects unauthenticated users to the Frontend login (with redirect-back) instead of /login. Added NEXT_PUBLIC_FRONTEND_AUTH_URL to .env/.env.example (dev=http://localhost:3000). | scope: lib/auth.ts, contexts/AuthContext.tsx, app/(auth)/login/page.tsx, app/middleware.ts, .env(.example) | why: single login UI on the Frontend; shared cross-subdomain cookie keeps the session valid on both apps. NOTE: hooks/useAuth.ts (useLogin) + services/auth.service.ts are now orphaned (login form removed) — left in place, not deleted. NOTE: app/middleware.ts is named `proxy` and lives under app/ — it likely never executes (Next expects root/src `middleware`); client-side AuthContext is the effective gate. Flagged, not fixed.

2026-06-18 — Auth follow-up cleanup: (1) Fixed the never-running middleware: it was at app/middleware.ts (Next only loads proxy/middleware from root or src/), so auth gating was client-only. Moved to root and renamed to Next 16's convention — file proxy.ts exporting `proxy` (the deprecated `middleware` name compiled but warned). Build now shows "ƒ Proxy (Middleware)" registered, no deprecation warning; it bounces unauthenticated requests to the Frontend login. (2) Deleted orphaned login code left after the admin login form became a redirector: hooks/useAuth.ts (useLogin) and services/auth.service.ts (no remaining importers). Left types/index.ts LoginResponse (shared API type, not orphaned by this). | scope: proxy.ts (was app/middleware.ts), deleted hooks/useAuth.ts + services/auth.service.ts | why: make server-side auth gating actually execute under the correct Next 16 file convention, and remove dead login form code.
2026-06-19 — Added a bundle media section to combo create/edit with a single `bundle_thumb_4_3` upload field, and widened bundle `chips` typing so thumbnail data can be stored and preserved alongside other bundle metadata. | scope: components/bundles/BundleForm.tsx, services/bundle.service.ts | why: admin now needs one shared bundle thumbnail that the student frontend can consume directly.
2026-06-19 — Reworded combo publish state labels from `Live` to `Published` / `Unpublished` in the admin combo form and detail view. | scope: components/bundles/BundleForm.tsx, app/(dashboard)/combos/[comboid]/page.tsx | why: combo publication is an authoring state, not a runtime live session, so `Published` reads more accurate.
2026-06-29 — Added the MathPro logo mark beside the "Math Pro" text in the sidebar header (next/image); copied the logo into public/logo-mark.png. | scope: components/layout/Sidebar.tsx, public/logo-mark.png | why: surface the brand logo in admin, which previously rendered only a text wordmark and had unused logo files in public/.
- Quiz/LaTeX: removed LaTeXPlugin DOM-walker (it mutated Lexical-owned DOM, corrupting saved `$...$` delimiters); equations now stay as source text in the editor and render via RichPreview using the shared renderLatexInElement, mirroring MathPro_Frontend SafeHtmlRenderer. Quiz seed stays 4 options (variable, min 2).
- Quiz option delete fix: option Card/LexicalEditor keys were index-only, so React reused mounted Lexical instances after a removal and stale text was written back (delete appeared to do nothing). Keys now include options.length to force remount; handleAdd/RemoveOption read the target question by id instead of activeQuestion.
- Admin favicon regenerated from public/logo-mark.png (was an unrelated dark circle/triangle mark). Originals backed up in session scratchpad.
- RichPreview intermittent LaTeX: pairing dangerouslySetInnerHTML with a post-render KaTeX pass raced — KaTeX mutates the DOM so it diverges from the `processed` string React tracks, and the effect was keyed on `processed`, so a commit that didn't change it left raw `$...$` on screen until an unrelated edit. Now the component owns the container: useLayoutEffect re-seeds innerHTML then renders, every commit (idempotent, pre-paint).
